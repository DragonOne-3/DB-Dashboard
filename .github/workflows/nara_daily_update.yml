name: Daily Procurement Data Bot

on:
  schedule:
    # 한국 시간 기준 매일 07:13 (UTC 22:13)
    - cron: '13 22 * * *'
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60 

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests google-api-python-client google-auth-httplib2 google-auth-oauthlib pytimekr

      - name: Run script
        id: run_script
        env:
          DATA_GO_KR_API_KEY: ${{ secrets.DATA_GO_KR_API_KEY }}
          GOOGLE_AUTH_JSON: ${{ secrets.GOOGLE_AUTH_JSON }}
        run: python main.py

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "[조달 DATA] 수집 결과 리포트 (${{ steps.run_script.outputs.collect_date }})"
          to: mhy9680@naver.com
          from: 조달데이터봇 <jetti4856@gmail.com>
          html_body: |
            <div style="font-family: 'Malgun Gothic', sans-serif; line-height: 1.6;">
              <h3>🏛️ 조달청 데이터 자동 수집 리포트</h3>
              <ul>
                <li><b>수집 날짜:</b> ${{ steps.run_script.outputs.collect_date }}</li>
                <li><b>상태:</b> ${{ job.status }}</li>
              </ul>
              
              <hr style="border: 0.5px solid #eee;">
              ${{ steps.run_script.outputs.notice_info }}
              
              <hr style="border: 0.5px solid #eee;">
              <div style="background-color: #f9f9f9; padding: 10px; border-radius: 5px; white-space: pre-wrap;">
                ${{ steps.run_script.outputs.school_info }}
              </div>
              
              <br>
              ${{ steps.run_script.outputs.servc_info }}
              
              <hr style="border: 0.5px solid #eee;">
              <p style="font-size: 12px; color: #888;">본 메일은 시스템에 의해 자동으로 발송되었습니다.</p>
            </div>
