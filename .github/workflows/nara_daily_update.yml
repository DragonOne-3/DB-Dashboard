name: Daily Procurement Data Bot

on:
  schedule:
    # 한국 시간 기준 매일 06:13 (UTC 21:13)
    - cron: '13 21 * * *'
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60 

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas requests google-api-python-client google-auth-httplib2 google-auth-oauthlib pytimekr

      - name: Run script
        id: run_script
        env:
          DATA_GO_KR_API_KEY: ${{ secrets.DATA_GO_KR_API_KEY }}
          GOOGLE_AUTH_JSON: ${{ secrets.GOOGLE_AUTH_JSON }}
          DEFENSE_ORG_LIST: ${{ vars.DEFENSE_ORG_LIST }}
        run: python main.py

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          # 제목 날짜 변동 가능
          subject: "[조달 리포트] ${{ steps.run_script.outputs.collect_date }} 수집 결과"
          to: mhy9680@naver.com
          from: 조달데이터봇 <jetti4856@gmail.com>
          # 🚀 [핵심 수정] 아래 한 줄로 모든 리포트가 전송됩니다.
          html_body: |
            ${{ steps.run_script.outputs.full_report }}
