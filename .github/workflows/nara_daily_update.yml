name: Daily Procurement Data Bot

on:
  schedule:
    # 한국 시간 기준 매일 07:00 (UTC 22:00)
    - cron: '13 22 * * *'
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies # 💡 [루이튼 최종 수정] 이 부분을 이렇게 변경했습니다!
        run: |
          python -m pip install --upgrade pip # pip를 최신 버전으로 업데이트하는 좋은 습관
          pip install -r requirements.txt     # requirements.txt에 명시된 모든 의존성을 설치

      - name: Run script
        id: run_script
        env:
          # Secrets에 저장한 변수를 환경변수로 주입
          DATA_GO_KR_API_KEY: ${{ secrets.DATA_GO_KR_API_KEY }}
          GOOGLE_AUTH_JSON: ${{ secrets.GOOGLE_AUTH_JSON }}
        run: python main.py

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587             # 465 대신 보안 검사가 더 유연한 587번 포트 사용
          secure: false                # 587 포트를 사용할 때 필요한 설정
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "🤖 [조달 DATA] 특정품목 납품 DATA ${{ steps.run_script.outputs.collect_date }} 수집 결과"
          # 수신인에 본인 메일(jetti4856@gmail.com)도 추가해서 내가 잘 받는지 먼저 확인해보세요.
          to: mhy9680@innodep.com, jinseok8809@innodep.com, jetti4856@gmail.com
          # 발신자 이름을 메일 주소와 동일하게 설정 (수신 차단 확률을 낮춤)
          from: jetti4856@gmail.com
          html_body: |
            <div style="font-family: 'Malgun Gothic', sans-serif; line-height: 1.6;">
              <h3>🏛️ 조달청 데이터 자동 수집 리포트</h3>
              <ul>
                <li><b>수집 날짜:</b> ${{ steps.run_script.outputs.collect_date }}</li>
                <li><b>총 저장 건수:</b> ${{ steps.run_script.outputs.collect_count }}건</li>
                <li><b>상태:</b> ${{ job.status }}</li>
              </ul>
              <hr style="border: 0.5px solid #eee;">
              <div style="background-color: #f9f9f9; padding: 10px; border-radius: 5px; white-space: pre-wrap;">
                ${{ steps.run_script.outputs.school_info }}
              </div>
              <br>
              ${{ steps.run_script.outputs.servc_info }}
              <hr style="border: 0.5px solid #eee;">
              <p style="font-size: 12px; color: #888;">
                상세 로그 확인: <br>
                <a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">
                  ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
                </a>
              </p>
            </div>
